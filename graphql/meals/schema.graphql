type Query {
  meals(search: SearchInput!): [Meal]
  findMealById(_id: String!): Meal
}

type Mutation {
  createMeal(name: String!, image: Upload!): Meal
  updateMeal(_id: ID!, name: String!, image: Upload!): Boolean
  removeMeal(_id: ID!): Boolean
}